version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - flutter doctor
            - flutter pub get
        build:
          commands:
            - flutter build web --release
      artifacts:
        baseDirectory: build/web
        files:
          - '**/*'
      cache:
        paths:
          - ~/.pub-cache/**/*
          - .dart_tool/**/*